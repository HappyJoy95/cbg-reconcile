# 运维手册（部署 / 维护用）

> ⚠️ **这份不发门店** —— 641 行太厚，门店看的是精简版 `门店操作手册.md`
> （五六步，打包时进包的就是那份）。这份给部署的人、维护的人、以及排查问题时查。

> **一句话**：这个工具每天自动比对「云商今天卖的、归属本店的串号」有没有都报量给华为，
> 没报的列成一张差异清单。

---

## ⚠️ 先看这个

**包里一个真凭据都没有。** `.secrets/erp.env` 是**空模板**（键名齐、值全空），
云商账号密码要在门店电脑上填一次 —— 见第 3 步。所以这个包**可以正常转发**，
不用紧张。

> 以前这里写过"包里带了云商账号密码，别外传" —— 那是**旧行为**，早就改了。
> 现在打包脚本只放空模板（`tools/build_package.sh` 里写着为什么）。

华为那边**不带任何凭据** —— 需要在门店电脑上**登录一次**（见第 3 步），
之后工具会自己续期，不用再管。

---

## 改了门店会怎样？

「设置 → 门店」那三行（`store_code` / `marker` / `erp_store_name`）**随时能改，
改完立刻生效，不用重启**。但有两件事要知道：

| 改了什么 | 会发生什么 |
|---|---|
| `marker`（串号标识） | 下次跑就按新的标识筛，立刻生效 |
| `erp_store_name`（云商门店名） | 同上 |
| **`store_code`（华为门店编码）** | ⚠ 会话文件是按店存的（`.secrets\cbg-<门店码>.json`），**换店等于换了一份会话** —— 会话页会显示"还没有会话"，需要用**那个店的华为账号**重新登录一次 |

> **顺序上唯一要注意的**：先把门店配对，**再加定时任务**。
> 不然定时任务到点那一跑，会拿错的门店去对账 —— 而且**看起来一切正常**，
> 不报错。跑一次 `selftest.bat`，第 0 节会打印当前配的是哪个店。

---

## 先拿到包

**门店用正式包**：发 `dist/cbg-reconcile-v<版本>-<日期>.zip`（旁边有个 `.sha256`，
拷过去之后能确认没坏）。包里已经备好 `BUILD.txt` 版本指纹、`.secrets/` 空模板、
`out/` 目录和发布说明，自检、诊断、卸载脚本也一应俱全。

在**装了 git 的机器**上（开发机 / 测试机）也可以直接 clone，见
[clone 部署](#clone-部署开发机--测试机) —— 但门店**不建议**，两边的差别那一节写了。

---

## clone 部署（开发机 / 测试机）

在**装了 git 的机器**上可以不走压缩包，直接：

```bat
git clone https://github.com/HappyJoy95/cbg-reconcile.git D:\cbg-reconcile
```

然后**双击 `install.bat`** —— 跟解压正式包之后完全一样，没有额外步骤。
`install.bat` 检测到这个目录有 `.git`、又没有 `BUILD.txt` 时，会自己补一个
构建指纹，所以控制台和 `selftest.bat` 第 0 节照样能看出跑的是哪个 commit：

```
版本　 CBG报量对账 v1.2.0 · git main@d0b9f80
```

### 门店为什么**不建议**这么做

| | 正式包（zip） | clone |
|---|---|---|
| 自动更新能用吗 | ✅ | ✅ **一样**（见下） |
| 更新之后 | 干净 | `git status` 一片改动，之后 `git pull` **必然冲突** |
| 多带的东西 | 干净 | `.gitignore`、`.gitattributes`、`tests\` 等一堆开发文件 |
| 门店要装 git | 不用 | 要，而且 clone 时就得连上 github.com |
| 拷过去坏没坏 | `.sha256` 能验 | 没法验 |

**「自动更新」跟安装方式无关**：控制台的「检查更新」是去 GitHub 下载代码包
**覆盖文件**，它不碰本地 git、也不调 git 命令。所以 clone 装出来的机器，
更新效果和解压装出来的一模一样 —— 但更新完目录会变脏，这是 clone 唯一的代价。

### 三条注意事项

1. **`git pull` 和「检查更新」别混着用。** 更新走的是覆盖文件，提交指针没动，
   所以 pull 会冲突。要么一直用 `git pull`（并接受它带来的一切），
   要么一直用控制台的「检查更新」。
2. **`--depth 1`（浅克隆）也可以**，不影响任何功能 —— 只是 `.git` 里没有历史。
3. **clone 不会带来凭据。** `.secrets/` 和 `config/store-<门店码>.yaml` 都在
   `.gitignore` 里，仓库里根本没有 —— 该填的还是要填（第 3 步）。
   ⚠ 但要注意优先级：`~/.dsh/secrets/erp.env` **也会被读**，顺序是
   **环境变量 > 项目里的 `.secrets/erp.env` > `~/.dsh/secrets/erp.env`**。
   所以那台机器上要是躺着一份别人的 `~/.dsh/secrets/erp.env`，会被**静默**用上。
   跑一次 `selftest.bat` 第 3 节会告诉你凭据是从哪个文件读的。

---

## 三步开始

### 第 1 步：装 Python（只需一次）

1. 去 https://www.python.org/downloads/ 下最新版 **Python 3.14**（3.9 以上都行，已实测 3.14）
2. 安装时**务必勾选 `Add python.exe to PATH`**（在最下面，很容易漏）
3. 装完**双击 `install.bat`**（需要联网，大约 1 分钟）：
   * 先装依赖（requests / pyyaml / openpyxl）
   * 装完会问一句「**设为开机自动启动？[Y/n]**」—— **直接回车**就行

> 开机自启 = 每次登录这台电脑，服务自动在后台跑起来（不弹窗），
> 店员直接开浏览器就能看报告，不用记得去双击 start.bat。
> 想跳过就输 `n`（之后也能在「设置 → 后台服务」里补开）。

### 第 2 步：启动（后台运行）

> 四个 `.bat` 里**没有中文**（提示由 Python 打印）。
> 这不是偷懒 —— 批处理的编码受控制台代码页摆布，
> 写死 GBK 在某些机器上会显示成方块，写死 UTF-8 在另一些机器上也会。
> 交给 Python 打印就没这个问题（Windows 上走 `WriteConsoleW`，跟代码页无关）。

**双击 `start.bat`**，会看到：

```
============================================
  云商 - 华为报量对账
============================================

 正在后台启动服务...
 等待服务就绪...

 ============================================
   启动成功，服务已在后台运行
 ============================================

   控制台： http://127.0.0.1:8787
   浏览器马上自动打开；没开就手动复制上面的地址

   停止服务：双击 stop.bat
   这个窗口关掉不影响后台服务

 按任意键关闭这个窗口...
```

**按任意键，黑窗就消失了** —— 服务继续在后台跑，浏览器已经打开控制台。

* 再双击 `start.bat` **不会起第二个** —— 会认出已经在跑，直接开浏览器
* 停止服务：双击 **`stop.bat`**
* 想让它**开机就自动起来**：见下面「第 8 步」

### 第 3 步：登录华为 + 配账号 + 配门店

**① 登录华为**（控制台 →「会话」页）

1. 填「**华为账号**」「**密码**」→ 点「保存账号」
2. 点「**打开浏览器抓取**」

工具会打开门店日常入口，没登录会自动跳到华为 SSO，然后**自动填表登录** ——
不用手点，也不用抄 curl。抓完窗口自动关闭。

> **用哪个浏览器**：这台电脑装了 Chrome 就用 **Chrome**，没装才用 Edge
> （Edge 是 Windows 自带的，所以一定有个能用的）。
> 想换成 Edge，在 `config\store-<门店码>.yaml` 里加一行：
> ```yaml
> browser:
>   prefer: [edge, chrome]
> ```
> ⚠ **换浏览器后要重新登录一次华为** —— 登录态存在浏览器 profile 里，
> 换一个浏览器等于换了套登录态。

> 不填账号密码也能用：那样会打开窗口等你手动登一次。
> 遇到**图形验证码**（连续失败几次后会出现）时自动登录会停下，窗口留着让你手动登，
> 登完 cookie 照样会被自动抓走。

**② 配云商账号**（控制台 →「设置」页 →「云商账号」）

包里**已经带了**账号密码，正常情况不用动。如果账号不对：

1. 填「账号」「密码」「公司代码」（公司代码默认 `00001937`）
2. 点「**测试登录**」—— 真去云商换一次 token，成功会显示登录人

> ⚠️ **别连着点「测试登录」**。云商连着登几次会要求图形验证码，那时会报
> 「该账号需要图形验证码」。**歇几分钟再试**就好。
>
> 万一一直过不了验证码：浏览器登录云商后按 `F12`，从任意请求的 `Authorization`
> 头里复制 `Bearer` 后面那串，填到「手动填 token」里存起来。

**④ 配邮件推送**（控制台 →「设置」页 →「邮件推送」）

**默认是关着的**（开关没勾），配一下就能用：

1. 勾上「每次跑完把差异清单发到邮箱」
2. 从「常见邮箱快速填充」里选一个（会自动填服务器/端口/加密方式）
3. 填账号、密码（多数邮箱要填**授权码**，不是登录密码）
4. 填收件人（多个用逗号分隔）
5. 点「**发送测试邮件**」验证 —— **失败不会保存**，填错了不影响已有配置

配好之后每次跑完都会自动发一封，带 xlsx 附件和正文摘要 ——
不用守着门店电脑看结果。

「发送时机」默认**每次都发**。嫌吵可以改成「只有差异才发」。

**⑤ 配企微群推送**（控制台 →「设置」页 →「企微群推送」）

在企业微信群里加个机器人，把 webhook 地址粘进来：

1. 群里 → 右上角设置 → **群机器人** → 添加机器人 → 复制 **webhook 地址**
2. 粘到「webhook 地址」（**整条**粘进来，含 `key=…`）
3. 点「**推送测试消息**」—— 群里收到就对了

**默认是关着的**（开关没勾），配一下就能用。
默认开着「有差异时 @所有人」和「同时把 xlsx 传到群里」。
企微的 markdown 不支持 @，所以有差异时会先发一条 @所有人的短消息，再发详情。

**⑥ 配门店**（控制台 →「设置」页 →「门店」）

改这三行，然后点「保存配置」：

| 字段 | 填什么 | 例子 |
|---|---|---|
| 华为门店编码 | 这家店的华为 SCN 编码（不知道就**留空**） | `SCN231409` |
| 串号标识 | 本店码，对照 `config\stores.yaml` | `Y` |
| 云商门店名 | 云商里的门店名 | `青岛新业广场店` |

> 14 家店的对应关系都在 `config\stores.yaml` 里，**那是唯一真源**。

**⑦ 跑一次**（控制台 →「运行」页 →「开始运行」）

默认跑**昨天**。跑完会生成 `out\差异_日期_门店码.xlsx`，**并自动发邮件 + 推企微群**。

---

## 怎么看结果

**每跑一次就存一份，不会覆盖**（文件名带生成时刻）。
所以同一个日期可能有好几份 —— 计划任务跑的一份、你手动补跑的一份。
控制台「报告」页可以逐份删。

差异清单有 5 个工作表：

| 工作表 | 看什么 |
|---|---|
| **未报量** | ⭐ **主要看的**：云商卖了、华为没报量的串号 |
| **反向差异** | 华为报了量、但不是本店标识的货 → **核对调拨过来的货有没有出库** |
| 已报量 | 两边都对上的（抽查用） |
| 过滤统计 | 各种跳过原因的条数（数字不对时看这里） |
| 运行信息 | 这次跑的区间、时间、配置文件 |

「反向差异」表里每条都带云商侧的出库记录：

* `✅ 已出库` + 卖出店/单号/金额 → 正常，货确实卖了
* `⚠️ 查无出库记录` → **要问一句：货呢？**

---

## 点「执行」之后会怎样？

定时任务表里每一行右边有个「执行」—— 点了就**按这条任务立刻跑一次**
（跟到点自动跑走的是同一条路）。跑的过程会**直接显示在下面**：

```
✅ 跑完了（退出码 0：没有差异）
  跟到点自动跑的结果完全一样。

[6/6] 企微：✅ 已推送到群（markdown + 文件）
[5/6] 邮件：跳过（配置的是「仅有差异时发」，本次无差异）

=== 2026-09-15 21:00:01 开始 ===
...（完整日志）...
[2026-09-15 21:01:12] exit=0
```

**「点了没推送」怎么看** —— 看中间那两行：

| 显示 | 含义 |
|---|---|
| `企微：✅ 已推送到群` | 推了 |
| `企微：跳过（配置的是「仅有差异时发」，本次无差异）` | **没推，但这是对的** —— 设置里选的就是"只有差异才推" |
| `企微：跳过（企微推送没开）` | 到「设置 → 企微群推送」里开一下 |
| `企微：跳过（配置不全：…）` | 缺 webhook 之类的，按括号里写的补 |
| `企微：❌ 推送失败 —— …` | 真失败了，看后面的原因（webhook 过期？网络？） |

邮件同理。

> 想改成每次都推：到「设置 → 推送」把「发送时机」改成「每次都推」。
>
> 退出码：`0` 没差异 · `3` 有差异（都算跑成功）· `1` 会话过期 · `2` 取数失败 · `9` 程序出错。

---

## 会话页怎么看

「会话」页的「当前会话」有三种状态，**别搞混**：

| 显示 | 含义 | 该做什么 |
|---|---|---|
| 🟡 会话文件在，但没验证过 | 只是**文件在**，不知道还能不能用 | 点右上「会话自检」 |
| ✅ 上次自检**通过** · 时间 | 真的验证过了（拿它去问了一次华为） | 没事，等着自动续期就行 |
| ❌ 上次自检**没过** · 时间 | 会话过期了 | 点「刷新会话」重新抓一份 |

下面那张表里还有一行「**上次自检**」，写的就是上次自检的时间和结果。

> **自检结果是存下来的**（`.secrets\session-check.json`），刷新页面、重启服务都还在 ——
> 不用每次打开都重新点一遍。
>
> ⚠️ 但**换过会话之后，旧的自检记录会自动作废**（界面会提示"要重新自检一次"）。
> 这是故意的：拿旧会话的"✅ 通过"给新会话背书，比不显示更糟。

---

## 双击 bat 只停在那儿等按回车？

**如果窗口里是一段英文说明，那不是卡住 —— 是它在告诉你"找不到 Python"。**

```
============================================================
 NO WORKING PYTHON FOUND -- this script cannot continue.

 Tried:
   python -c "import sys"      (failed)
   py -3  -c "import sys"      (failed)

 What "where python" found:
   (nothing listed above = python is not on PATH at all)

 COMMON CAUSE: the Microsoft Store "python" is a STUB, not Python.
 ...
 FIX:
   1. Install Python 3.14 from   https://www.python.org/downloads/
   2. During setup TICK the box  "Add python.exe to PATH"
```

这段只能是**英文** —— 中文全部由 Python 打印，而这时候恰恰没有 Python 可用。

**为什么会这样**：Windows 自带一个假的 `python`（Microsoft Store 的"应用执行别名"）。
双击 bat 时它会弹一下商店、什么都不干。所以每个 bat 现在会先**探一下 python 到底能不能用**，
不能用就停下来把话说清楚，而不是像以前那样"闪一下就没了"。

> **先装了 Python 但忘了勾 `Add python.exe to PATH`？**
> 新版本会自动改用 `py` 启动器（Windows 一般都装了），不需要重装。

---

## 出问题了先跑 `diagnose.bat`

**双击 `diagnose.bat`**，它会在同目录生成 `diagnose-result.txt`，把这个文件发回来就行。

它**不需要 Python 也能跑** —— 要查的恰恰可能就是"Python 没装好"，那时候任何
Python 脚本都起不来。

里面会写：

| 查什么 | 为什么 |
|---|---|
| `where python` / `where pythonw` | Python 到底在哪；**是不是 Microsoft Store 那个假的** |
| `python -V` | 版本对不对（要 3.14） |
| `import requests, yaml, openpyxl` | 依赖装了没 |
| `run.bat` 全文 | 里面写的 Python 路径还在不在 |
| `out\run.log` 全文 | 上次跑到哪、报了什么错 |
| 计划任务列表 | 任务注册成什么样 |
| **真跑一次对账** | 这条最关键 —— 错误会直接被抓进文件 |

---

## 黑色命令行窗口能关掉吗？

**能，已经处理了。**

以前跑对账的时候屏幕上会挂一个**黑色 cmd 窗口**（一两分钟），很影响观感。两个来源都修了：

| 什么时候 | 以前 | 现在 |
|---|---|---|
| 点「运行」/「执行」 | 黑窗挂满整个过程 | **完全不出现**（用 `CREATE_NO_WINDOW` 起子进程） |
| 到点自动跑 / 点「执行」 | 同上 | **只闪一下**（cmd 立刻退出，实际干活的 `pythonw.exe` 本身没有窗口） |
| 打开控制台翻页 | 每次查任务列表都闪一下 | **完全不闪** |

> 原理：服务是用 `pythonw.exe` 跑的（它本身没有控制台）。这时再起一个**控制台程序**
> （`schtasks`、`tasklist`、跑对账的 `python.exe`），Windows 会给它**新开一个黑窗**。
> 所以每个子进程都明确带上了"不要窗口"的标志。

想看到过程的时候，用 **`run-now.bat`** —— 那个是**故意**带窗口的，还能看到实时输出。

---

## 想手动跑一次？（`run.bat` 和 `run-now.bat`）

对账脚本有两个，**别搞混**：

| 文件 | 给谁用 | 行为 |
|---|---|---|
| `run.bat` | **计划任务**（也生成了 `run.sh`） | 跑完窗口自己关 |
| `run-now.bat` | **你自己双击** | 跑完**停住**，等你看完按任意键 |

两个都会把过程**同时**打到屏幕上和 `out\run.log` 里。

> **为什么之前双击 `run.bat` "什么都没发生"？**
> 老版本把输出全用 `>> out\run.log` 重定向走了 —— 屏幕上真的一片空白，
> 跑一分多钟然后窗口自己关掉，完全判断不了跑没跑。现在改成 Python 那边分流，
> 屏幕和日志两边都有。

**手动跑建议双击 `run-now.bat`**（它会停住）。跑完看最后那行：

```
退出码 0   ← 正常
退出码 1   ← 华为会话过期，到「会话」页重新登录
退出码 2   ← 取数失败（网络 / 云商登录）
退出码 3   ← 有差异（这是好事，报告在 out\ 里）
```

界面上的「立即运行」和定时任务表里的「执行」按钮也能跑，还不用看黑窗口。

---

## 让它每天自动跑

「设置」页 →「定时执行」→ 选时间 →「注册计划任务」。

注册完，上面的表会**列出这台电脑上所有对账任务**（每天几点、启用没启用、跑的是哪个脚本），
每行右边一个「删除」按钮 —— **删除只删你点的那一行**，别的任务不受影响。

**时间点选店里肯定开着机的时候** —— 门店一般设**晚上关门前**（比如 21:00）：

> ⚠️ Windows 计划任务**错过时间不会补跑**。设在半夜或清晨，那天电脑没开机就不跑了，
> 而且**不会有任何提示**。设在关门前就稳了。
>
> 万一漏了可以手动补：界面「运行」页选那个日期跑一次，
> 或 `python -m src.cli check --date 2026-09-14`。

**「昨天」还是「今天」？**

| 选项 | 好处 | 代价 |
|---|---|---|
| **昨天**（默认） | **零遗漏** —— 那天的销售早就全部发生完了 | 发现漏报晚一天 |
| 今天 | 当晚就能发现今天的漏报，来得及让店员补 | **跑完到打烊之间的销售不在这份报告里**（会出现在次日那份里） |

### 想一天跑两次？

**把时间改一下，再点一次「注册计划任务」就行** —— 任务名留空会**自动带上时间**：

| 你选的时间 | 自动生成的任务名 |
|---|---|
| 12:00 | `CBG报量对账-12点00` |
| 21:00 | `CBG报量对账-21点00` |

所以两次注册是两个不同的任务，上面那张表会都列出来，各跑各的。

> **为什么是「21点00」不是「21:00」**：Windows 计划任务的名字**就是文件名**
> （存在 `C:\Windows\System32\Tasks\` 下），冒号是非法字符，用了直接注册失败。
>
> 想自己起名就在「任务名」框里填；**同名**再注册一次是**覆盖**（不会变成两个）。
> 名字里不能用 `\ / : * ? " < > |`。

> ⚠️ **升级提醒**：如果你之前注册过，表里可能还留着一条名字就叫 `CBG报量对账`
> （没有时间后缀）的**旧任务** —— 它和新任务会**各跑一次，等于一天对账两遍**。
> 界面会用黄条提示你，不需要的话点那一行的「删除」。

### 开机自启没设上？（第 8 步）

安装时如果跳过了（输了 `n`），或者后来才发现没设，可以补：

「设置」页 →「**后台服务**」→ 勾上「**开机自动启动**」→ 保存。

命令行也行：**右键 `install.bat` →「以管理员身份运行」**，或
在管理员命令行里跑 `python bootstrap.py autostart`。

> **它是"以管理员身份"跑的** —— 这样界面上注册计划任务、写程序目录都不会再报权限错。
> 用的是**登录触发的计划任务**，所以**只在注册那一下弹一次 UAC，以后每次开机都不弹**。
>
> ⚠️ 如果注册时没给管理员权限（UAC 点了"否"，或者账号本来就不是管理员），
> 会退回注册表启动项 —— 服务照跑，但**是普通权限**。
> 界面「设置 → 后台服务」里会明确写「⚠️ 普通权限（不是管理员）」，看到这行就按上面的办法重来一次。
>
> 在这台电脑上换过项目目录的话，回这里重新保存一次即可（界面会提示"注册的还是老路径"）。

**怎么确认真的提权了？** 「设置 → 后台服务」里看这一行：

| 显示 | 含义 |
|---|---|
| ✅ 计划任务（登录触发）· 以管理员身份运行 · 不弹 UAC | 对了 |
| ⚠️ 注册表启动项 · **普通权限**（不是管理员） | 要按上面的办法重来一次 |

> 另外，服务是管理员身份时，**普通双击的 `stop.bat` 正常也能停**（走的是服务自己退出，
> 跟权限无关）。只有服务卡死了才需要**右键 `stop.bat` →「以管理员身份运行」**。

**计划任务和开机自启是两件事**：

| | 管什么 |
|---|---|
| 定时执行（计划任务） | 每天几点**跑对账** |
| 开机自启 | 登录后**控制台**自动在后台起来，店员随时能打开看 |

两个都设上才算完全无人值守。

---

## 出问题了先跑 `selftest.bat`

它会依次检查：单元测试 / 浏览器 / 云商凭据 / 云商登录 / 华为会话。

### 常见情况

| 现象 | 怎么办 |
|---|---|
| **双击 bat 停在英文说明上等回车** | **不是卡住** —— 是在说找不到可用的 Python。按屏幕上 `FIX` 那三步行事 |
| 双击 bat 闪一下就没了 | 说明 Python 没装好或没加 PATH。右键 bat → 以管理员身份运行看报错，或重装 Python 勾上 Add to PATH |
| 双击 `install.bat` 弹出了**微软商店** | 那是 Windows 的假 `python`（应用执行别名），不是真 Python。装真 Python 3.14 并勾上 Add python.exe to PATH。`install.bat` 会提示这一段（英文，因为这时没有 Python 能打中文） |
| 提示「Python 版本太旧」 | 按提示装新版。门店统一装 **3.14**（3.9 以上都行，已实测） |
| 想知道跑的是什么环境 | 双击 `selftest.bat`，第 0 节会列出 Python 版本、系统、三个依赖的版本 —— 出问题时把这几行发过来就够了 |
| `[X] 没找到 python` | 同上，重装 Python 并勾选 Add python.exe to PATH |
| 依赖装不上 | 换镜像：`python -m pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple` |
| 云商报「需要图形验证码」 | **照页面上显示的图填一下就行** —— 验证码会直接出现在设置页。填错会自动换新图 |
| 云商登录失败说「账号或密码不对」 | 到「设置 → 云商账号」核对，点「测试登录」验证 |
| **邮件发不出去** | 先点「发送测试邮件」看具体报错。最常见两种：①**密码填的是登录密码，要填授权码**；②端口/加密方式配错（SSL 用 465，STARTTLS 用 587） |
| **邮件进垃圾箱** | 把发件人加进白名单 |
| **企微推不出去** | 点「推送测试消息」看报错。`webhook 地址不对`= 地址粘错或机器人被删；`发得太频繁`= 企微限流（每分钟 20 条），等一会儿 |
| 邮件/企微失败会不会影响对账 | **不会**。对账结果是主产物，推送只是投递方式 —— 推送挂了照样出差异清单，只是少一条消息 |
| 「没找到 Chrome / Edge」 | Windows 10/11 自带 Edge。如果被卸载了，装回 Edge 或 Chrome 即可 |
| 「需要人工登录一次」 | 华为 SSO 过期了。控制台「会话」页点「打开浏览器抓取」再登一次（配了账号密码就是全自动的） |
| 自动登录没成功 | 可能碰上了图形验证码。窗口会留着，在里面手动登一次即可 |
| 华为账号密码填哪 | 控制台「会话」页，只存本机 `.secrets/huawei.env`，界面不回显 |
| 报「会话已过期」但自动续期没成 | 同上，人工登录一次 |
| 未报量数量看着不对 | 打开报告的「过滤统计」表，看是不是门店三行配错了 |
| 端口被占用 | 控制台启动时会自动换端口。用 `python -m src.cli status` 看实际端口 |
| **某天没生成报告** | 大概率**当时电脑没开机**（计划任务错过时间不补跑）。所以时间点要设在店里肯定开着机的时候。漏了手动补：界面「运行」页选那个日期，或 `python -m src.cli check --date 2026-09-14` |
| 怎么确认今天到底跑没跑 | 看 `out\` 里有没有对应的报告文件；报告**每次跑都存一份**，所以"没有文件"就是"没跑" |
| `start.bat` 说「启动失败」 | 先跑 `install.bat` 装依赖；再跑 `selftest.bat` 看详细报错 |
| 关掉黑窗后服务还在吗 | **在**。黑窗只是前台提示，服务是另起的后台进程 |
| 怎么知道服务在不在 | 双击 `start.bat` 会说；或命令行 `python -m src.cli status` |
| 定时任务表里「执行时间」显示"时间没读出来" | 任务还在、也能正常跑，只是这台电脑的 `schtasks` 没把配置读出来。照着表里的任务名去「任务计划程序」里看一眼时间对不对即可；删掉重新注册一次通常就好了 |
| 注册了两个任务，删一个会不会把另一个也删了 | **不会**。每行右边各有一个「删除」，删的就是那一行 |
| 开机自启没生效 | 到「设置 → 后台服务」看状态。提示"注册的还是老路径"就重新保存一次 |
| **自动抓会话报「Chrome 启动后立刻退出（退出码 21）」** | 服务是管理员身份时，Chrome 138+ 会**把自己降权重启**。新版已经能容忍这个过程；如果还报，就按报错里列的「可能的原因」逐条试。**最有效的一条**：「设置 → 后台服务」→「启动方式」改成「**普通权限**」→ 保存 → 再双击 `stop.bat` 停掉、`start.bat` 重新起来（不用管理员） |
| 界面显示的构建时间跟我以为的不一样 | 那台电脑上跑的是旧版本。按上面「升级到新版本」重新覆盖一次 |
| **双击 `run.bat` 什么都没发生** | 它是给计划任务调的，跑完窗口自己关。手动跑请双击 **`run-now.bat`**（会停住）。两个都把过程写进 `out\\run.log` |
| 报「Chrome 启动后立刻退出（退出码 21）」且提示 **profile 被占着** | 有 Chrome/Edge 实例正用着这个 profile。**关掉所有 Chrome/Edge 窗口**；找不到窗口就打开任务管理器，把残留的 `chrome.exe` / `msedge.exe` 结束掉。这个坑会连锁 —— 上次没关掉的浏览器会让之后每次抓取都失败 |
| 想确认服务是不是管理员身份 | 只能从「设置 → 后台服务」那行字看（开机自启注册的任务我们读的是 `RunLevel`）。显示「注册表启动项 · 普通权限」就右键 `install.bat` 以管理员身份重跑一次 |
| 点「保存」后弹了 UAC | 正常的，**只在注册这一步弹一次**。点是；点否就退回普通权限，界面会标出来 |
| 忘了设开机自启 | 命令行 `python -m src.cli autostart --on`，或到「设置 → 后台服务」勾 |
| 安装时那问题能重来吗 | 能。重跑 `install.bat` 不会再问（已设过就直接跳过）；要改去「设置 → 后台服务」 |

---

## 卸载

双击 **`uninstall.bat`**。它会：

1. 停掉后台服务
2. 删掉**所有**定时任务（每天对账的 + 开机自启的）
3. 删掉开机自启的注册表项
4. 问你要不要连 `.secrets\`（账号/会话）和 `out\`（报告）一起删

> **默认保留报告和凭据** —— 删了就恢复不了，所以要多答一次 `y` 才删。
> 想一步到位（无人值守）就命令行跑：`python bootstrap.py uninstall --yes --purge`
>
> 程序目录本身**不会**被删（脚本删不掉自己所在的目录），最后会打印路径让你手动删。

---

## 升级到新版本

> **正常情况下不用看这一节** —— **控制台每天自己查一次**有没有新版本，
> 发现了就在「设置」里挂提醒（右上角也会出现一个「有新版本」徽章），
> 点一下「立即更新」就把代码换新了，门店配置 / 账号会话 / 历史报告都不会动。
>
> **它只提醒、不自动装**：更新完控制台要重启，正好撞上 21:00 那趟对账就把任务断了。
> 所以装不装、什么时候装，由店里决定。连不上 GitHub 时会如实写出来（不影响对账），
> 下面说的手工换包随时可用。
>
> 要看到这个提醒，控制台的页面得是新的 —— 老页面刷一下（`Ctrl+F5`）就有了。

**别把整个目录删掉重来** —— 那会丢掉你的配置、华为会话和历史报告。

### 推荐做法：换目录（最省事，不容易错）

1. 把新包解压到**别的地方**，比如 `下载\cbg-new\`
2. 把**旧目录**里的这三个，**复制到新目录**里：
   ```
   config\      ← 你的门店配置（门店名、华为账号…）
   .secrets\    ← 云商账号、华为会话、浏览器登录态
   out\         ← 历史报告
   ```
3. 把旧目录改名成 `cbg-reconcile-备份`，把新目录改名成 **`cbg-reconcile`**
4. 完事。计划任务和开机自启记的是**完整路径**，所以目录名要跟原来一样

> 备份目录先别删，跑两天确认没问题再删。

### 更保险的做法：只覆盖代码

不想换目录的话，就把新目录里这些**复制过去覆盖**：

```
src\  web\  tests\
bootstrap.py  boot.py  BUILD.txt  requirements.txt
install.bat  start.bat  stop.bat  selftest.bat
```

**绝对不要覆盖** `config\`、`.secrets\`、`out\`。

### 怎么确认升级成功了？

打开控制台，标题下面会显示版本和构建时间：

```
v1.1.0 · 2026-09-15 13:55
```

如果那里写着「**源码运行（未打包）**」，说明 `BUILD.txt` 没拷过去（走上面的"换目录"就不会漏）。

命令行也行：双击 `selftest.bat`，**第 0 节**会打印版本和 Python 版本。

---

## 命令行也能用

不想开控制台的话：

```bat
python -m src.cli auth --auto           :: 打开浏览器登录一次
python -m src.cli ping                  :: 会话自检
python -m src.cli check --days-ago 1    :: 跑对账（昨天）
python -m src.cli check --date 2026-09-14   :: 跑指定日期
```

**退出码**：`0` 正常 · `1` 会话过期 · `2` 取数失败 · `3` 有差异

---

## 目录说明

```
cbg-reconcile\
├── install.bat          装依赖（一次）
├── start.bat            启动控制台（后台运行，窗口报完就关）
├── stop.bat             停止后台服务
├── selftest.bat         出问题先跑这个
├── bootstrap.py         四个 bat 的统一入口（纯标准库，装依赖前就能跑）
├── boot.py              开机自启的入口（不用手动跑）
├── config\
│   ├── stores.yaml      14 家体验店 + 串号标识映射（唯一真源）
│   └── store-*.yaml     本机配置（界面上改就行）
├── src\                 代码
├── web\                 界面
├── tests\               单元测试
├── .secrets\            ⚠️ 凭据，别外传
└── out\                 差异清单 + 运行日志
```
