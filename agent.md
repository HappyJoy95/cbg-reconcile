# cbg-reconcile · agent.md

> **给在门店电脑上接手 2.0.0 开发的 agent 看的。** 先读这一页，再读 `AGENTS.md`
> （那本是项目的总规矩，这本只说"在这台机器上要干什么"）。
>
> 最后更新：2026-09-16，由开发机（macOS）上的会话整理。

---

## 0. 三十秒版

| | |
|---|---|
| **这是什么** | 比对「云商（盛联 ERP）里卖出的、归属本店的串号」有没有都报量给华为。装在门店 Windows 电脑上，每天定时跑 |
| **现在什么版本** | `1.6.0`（`src/version.py`），**已发正式包、门店在跑** |
| **已做了什么** | 报量对账完整可用；708 条测试三头全绿 |
| **2.0.0 要做什么** | ① 新增 **POS 使用率合规**（控制台单独一个 tab）② 新增 **SQLite 订单库**（从华为拉订单累积下来） |
| **你现在要做的第一件事** | §3「开工前必须先验的三件事」—— **只有这台机器能做** |

⚠ **在门店电脑上开发 = 在产线机器上开发。** 这台机器每天在跑定时对账，
改坏了门店就没对账结果。**先备份**（`xcopy /E /I /Y D:\cbg-reconcile D:\cbg-reconcile.bak-20260916`），
不确定就先在副本目录里试。

---

## 1. 在这台机器上先跑起来

```bat
python -m pytest tests/ -q          :: 708 条，约 27 秒。改完必须全绿
python bootstrap.py selftest        :: 逐项自检（版本 / 门店 / 依赖 / 会话 / 服务）
python -m src.cli serve             :: 起控制台 → http://127.0.0.1:8787
```

* 依赖只有三个：`requests` / `pyyaml` / `openpyxl`（`requirements.txt`）。
* **代码要求 Python 3.8+** —— 门店 Win7 老机器只能 3.8.10，所以别用 `match`、
  别写 `dict | dict`、注解用 `Optional[...]`。详见 `AGENTS.md` 开头那段。
* 开发机上三个 venv 在 `.dsh/tasks/venv38|venv39|venv314`（**`.dsh/` 不进 git**，
  这台机器上如果没有，自己建：`py -3.8 -m venv .venv38`）。
* 后台服务由 `pythonw.exe` 起（**没有控制台**）→ **别在启动路径用 emoji**（`AGENTS.md` 坑 2）。

---

## 2. 2.0.0 要做什么

**项目就两个功能**：报量对账（现状不动）+ POS 使用率合规（新）。

用户 2026-09-16 砍掉了原计划的"五项合规看板"——未收货激活率 / 在库激活率 /
离散率 / 当日激活的合规展示**全都不做**。别再往回加。

### 2.1 POS 使用率合规

| 问题 | 用户答复 |
|---|---|
| 版本号 | **2.0.0** |
| 在控制台怎么出现 | **单独一个 tab** |
| 使用率谁算 | **我们自己算**（口径用户另给，见 §3.3） |
| 数据从哪来 | **华为订单接口** —— 用户明确说："我们在华为那边拉的数据不就是 POS 单据吗" |

⭐ **所以 M1（探新接口）不用做了。** 数据源就是**现有的**
`POST /isrp/srs/sale-order/paged-list` + `POST /isrp/soms/sale-order`，
`src/cbg.py` 已经在用（"报量账本"和"唯一带 SN 的详情"）。

### 2.2 SQLite 订单库

从华为拉订单，存：**订单号 / 金额 / 产品 / SN / 备注**。用途是核算 POS 合规。

**为什么必须落库**（不是偏好）：`src/cbg.py` 顶部记着实测结论 ——
**`historyData=true` 直接报内部错误，只能 false**。华为接口**给不了任意历史**，
所以算一个周期的使用率只能**天天抓、自己累积**。

**放哪**：`out/cbg-orders.db`。⚠ **必须放在 `NEVER_TOUCH` 里的目录**
（`src/selfupdate.py:118`）：

```python
NEVER_TOUCH = ("config", ".secrets", "out", "dist", "tools", "__pycache__", ".dsh")
```

放 `data/` 这种新目录 → **门店自更新一次整库没了**。`out/` 已实测安全
（没有任何代码会整目录清它，`bootstrap.py:505` 负责建）。

**表结构（建议，可调）**：

```sql
CREATE TABLE IF NOT EXISTS cbg_orders (
    订单号   TEXT NOT NULL,      -- documentNo
    SN       TEXT NOT NULL,      -- details[].sn
    金额     REAL,               -- includedTaxAmount
    产品     TEXT,               -- details[].itemName
    备注     TEXT,               -- ⚠ 字段名待验，见 §3.1
    门店码   TEXT,               -- storeCode
    下单时间 TEXT,               -- docCreateTime
    抓取时间 TEXT,               -- 我们自己写的，用来追踪累积
    PRIMARY KEY (订单号, SN)
);
```

* **`INSERT OR REPLACE`（upsert）**：同一单重复抓不许产生重复行。
* ⚠ **`金额` 是订单级的**：一张单多个 SN 时，每行都会写同一个金额。
  **统计时必须先按 `订单号` 去重**，否则金额会翻倍。
* `sqlite3` 是标准库 → **零新依赖**，`requirements.txt` 不用动。

---

## 3. ⚠ 开工前必须先验的三件事（**只有这台机器能做**）

### 3.1 「备注」到底叫什么字段名

用户确认：**华为订单详情里本来就有备注**。
但 `src/cbg.py` 现在只取了这些字段：

```
documentNo / orderNo / storeCode / docCreateTime /
includedTaxAmount / consumerGuideName / salesAssistantId /
details[].sn / details[].itemName
```

**没有一个备注字段。** 所以这台机器上第一件事：抓一单真实详情，
把响应**完整打印出来**，找备注字段的真名（可能是 `remark` / `memo` / `comment` /
`description` 之类，别猜，看实际返回）。

⚠ 顺带确认：它是**订单级**还是**明细行级**的（决定它进不进 `details` 循环）。

### 3.2 历史到底能查多久

`historyData=true` 报内部错误 → 只能 `false`。但**能往前查几天**没测过。

→ 拿一个已知有单的旧日期试 `list_orders()`，看能不能拿到。
**这决定订单库要不要一次性补历史**，还是只能从今天开始累积。

### 3.3 使用率口径（**用户还没给**）

用户说"要自己算，我另给口径"，**还没给**。需要：**分子 / 分母 / 达标线 / 周期**。

### 3.4 ⭐ 先回答这个：POS 使用率 和 报量对账 的区别在哪

**用户 2026-09-16 被问到这条时的原话：「我也不确定，等我在门店机器上看一眼」。**
（也就是说 —— 这一条**要在这台机器上查清楚**，别直接开写。）

疑点是这样来的：

1. 用户说「**我们在华为那边拉的数据不就是 POS 单据吗**」；
2. 而 `src/cbg.py` 拉的那套接口，正是**报量对账**在用的（云商销量 vs 华为已报量）；
3. 如果两者同源，那"POS 使用率"和现有的报量对账**可能是同一个东西的两种说法**。

**怎么查**：在这台机器上跑一次对账（`python -m src.cli check`），
对着 `out/` 里那份报告看：华为侧那些订单，和门店理解的"POS 单据"**是不是同一批**。
再拿门店实际用的 POS 系统/页面比一比。

⚠ **写之前先确认**。写完了才发现和现有功能重复，是最亏的一种返工。

⚠ 真正需要用户给的是**口径**（分子/分母/达标线/周期）；
上面这条是"**别搞错对象**"，两件事不一样。

---

## 4. 已定的设计决定（别推翻）

1. **`src/reconcile.py` 的业务逻辑一行不改。** 708 条测试覆盖的就是它的口径，
   动它 = 把已验证的正确性换成未验证的重构。有改动只在它外面套。
2. **控制台新增 tab 的机制**（已读到行号级，照抄即可）：
   * `web/index.html:29-34` `<nav class="tabs">` 加 `<button class="tab" data-tab="pos">`
   * `web/index.html:38` 附近加 `<section class="panel" id="panel-pos">`
   * `web/app.js:80` 附近加一行 `if (b.dataset.tab === 'pos') loadPos();`
   * `src/web.py:465` 附近的 if 链加 `if path == "/api/pos" and method == "GET"`
   * ⚠ **`active` 类要从 `reports` 挪到新 tab**，否则新页面藏着不显示
   * ⚠ UI 复用现成的 `.kpi` / `.kpi.ok` / `.kpi.warn` / `.kpi.bad`（`web/style.css:79-86`），
     **CSS 一行不用加**
   * ⚠ **坑 3**：往表格/HTML 里放原生标签必须传 `{html: '...'}`，否则页面显示源码
3. **看板/接口只读落盘，不现场跑。** 一次合规检查要登云商 + 抓华为，
   几十秒到几分钟还可能弹验证码 —— 页面一打开就跑批 = 门店点一次卡一次。
   检查由定时任务触发，`/api/pos` **纯读盘、零网络**。
   （依据：`src/web.py:347` 现有注释"只看缓存，一个网络请求都不发"）
4. **落盘照 `src/report.py:99` 的「主文件 + 旁车 json」模式**（`list_reports()` 优先读旁车 json）。

---

## 5. 红线（详读 `AGENTS.md`，那本是真源）

* Python **3.8+**；不用 `match`、不写 `dict | dict`、注解用 `Optional[...]`。
* **无框架、无构建步骤**：`web/app.js` 改完刷新即生效，不引打包器。
* **708 条测试三头全绿**（3.8 / 3.9 / 3.14）才能提交。
* **自更新只碰代码**：`config/` `.secrets/` `out/` 一根手指不许碰。
* **包内不能有会覆盖门店设置的文件** —— 升级时整个目录拷过去就行。
* **默认不用管理员权限跑**（会弄坏自动抓会话，`AGENTS.md` 坑 7）。
* `.bat` 必须**纯 ASCII + CRLF**，中文一律由 Python 打印。
* **改完先别 push、别升版本号**，攒着等用户说"推吧"。要给别人测就打 beta 包。

---

## 6. 产物索引

⚠ **`.dsh/` 不进 git**。如果这台机器是 `git clone` 来的，**下面这些文档都不在** ——
要么从开发机把 `.dsh/docs/` 整个拷过来，要么就只靠本文件。

| 文档 | 讲什么 | 还有效吗 |
|---|---|---|
| `AGENTS.md` | **项目总规矩 + 九个踩过的坑** ← 必读 | ✅ |
| `.dsh/docs/2026-09-16-2.0.0-开发目标.md` | 2.0.0 的边界、里程碑、验收、风险 | ✅ |
| `.dsh/docs/2026-09-16-M1-华为POS接口探针.md` | 探针方案 | ⚠ **大部分作废**（数据源就是现有接口，用户已确认） |
| `.dsh/docs/2026-09-16-M2M3-集成设计.md` | 控制台集成点（行号级） | ⚠ **只有页签那半有效** |
| `.dsh/workspace/待纳入.md` | 一个半成品契约模块 | ⚠ **大部分作废，不要毕业** |
| `.dsh/memory/2026-09-16.md` | 当天的开发日志（决策与理由） | ✅ 想了解"为什么这么定"就看它 |

---

## 7. 别提交的东西

`.gitignore` 已经排除：`.secrets/`（云商账号、华为会话、浏览器登录态）、
`out/`（历史报告 + **新的订单库**）、`dist/`、`BUILD.txt`、`run*.bat`、
`config/store-*.yaml`（门店配置，现在只发模板 `src/store-config.default.yaml`）。

**凭据一个都不要贴进对话、不要提交。** 优先级：
环境变量 > `.secrets/erp.env` > `~/.dsh/secrets/erp.env`；
华为会话按店存 `.secrets/cbg-<门店码>.json`（**换店等于换一份会话**）。

---

## 8. 建议加载的 skill

| skill | 什么时候用 |
|---|---|
| `erp-api` | 要动云商取数时 —— 它是**云商接口契约的事实源**（含投影契约、哪些参数是真筛） |
| `using-superpowers` | 开工前扫一眼有没有现成能力，别重复造 |
| `systematic-debugging` | 抓不到数 / 报错不明时，别瞎试 |

> 这台机器上装了哪些 skill，`玲珑` 里自己列一下；上面是按开发机的清单给的。
