# 门店操作手册

> 这份是给门店同事看的，**只有五六步**。遇到这里没写的问题，
> 双击 `diagnose.bat`，把生成的 `diagnose-result.txt` 发回给维护的人就行。

---

## 一、装之前：这台电脑得有 Python

1. 打开 <https://www.python.org/downloads/> 下最新版
2. 装的时候**必须勾上最下面那个 `Add python.exe to PATH`** —— 很容易漏，
   漏了后面所有步骤都跑不起来
3. 已经装过就跳过这步

> 不确定装没装？双击 `install.bat`，它会自己检查。找不到 Python 时它会把
> 情况全打出来，照着提示做即可。

---

## 二、安装（四步）

1. **解压**到 `D:\cbg-reconcile`
   - 路径**别带空格**，**别放桌面**（用户名可能带空格，会出问题）
   - 解压后这个目录就别再挪了
2. 双击 **`install.bat`**
   - 会弹**一次**蓝色的 UAC 窗口，点「是」
   - 最后问「要不要开机自启」→ **直接按回车**（= 要）
3. 双击 **`start.bat`**
   - 看到「**启动成功，服务已在后台运行**」就是好了，浏览器会自己打开
   - 窗口里让你「按任意键」就按一下，**关掉窗口不影响服务**
4. 在控制台里配三样东西（见下一节）

> ⚠️ 如果窗口显示的是「**等待超时 —— 服务可能还在启动**」：**别慌，
> 这是正常的**。等 30 秒再双击一次 `start.bat` 就行（服务慢的时候会这样）。

---

## 三、控制台里要配的东西

打开控制台（双击 `start.bat`）→ 左边点「**设置**」：

| 配什么 | 在哪 | 说明 |
|---|---|---|
| **门店那三行** | 最上面「门店」卡片 | 华为门店编码 / 串号标识 / 云商门店名。**改完点下面那个「保存门店配置」** |
| **云商账号** | 「云商账号」卡片 | 填账号密码 → 点「测试登录」，成功了会显示登录人 |
| **邮件 / 企业微信**（可选） | 往下翻 | 要它每天自动推结果才填 |

然后点左边「**会话**」→ 填华为账号密码 → 点「**打开浏览器抓取**」：

- 会弹出一个浏览器窗口，**程序自己会填账号密码登录**
- **登录完别关那个窗口** —— 程序还要从它那里读东西，读完它自己会关
- 抓成功会显示「✅ 抓到 … 个 cookie」

> ⚠️ **华为账号必须是本店的。** 用别的店的账号能登上，但查不到本店的账
> —— 表现就是「登得上、抓不到」，一直转圈。每个店用自己的账号。

> ⚠️ **顺序很重要**：先把门店那三行配对、再抓会话、最后才设定时任务。
> 反了的话，到点那一跑会拿错门店去对账，**而且看起来一切正常、不报错**。

---

## 四、让它每天自己跑

控制台「设置」→ 找到「**定时执行**」→ 设一个时间 → 点注册。

**时间要选店里肯定开着机的时候**，一般设**晚上关门前**（比如 21:00）。

> Windows 的定时任务**错过时间不会补跑**。设成半夜或清晨，很可能一整天都不跑。

设完可以点一次「**执行**」试试，跑完会在「报告」页看到一份差异清单。

---

## 五、每天要看什么

平时不用管，它会自己跑。想确认结果：

- **「报告」页** —— 每次跑出来的差异清单都在这，点开能看、能下载
- 有差异的话，配了邮件/企微就会自动推给你

「没有差异」就是好消息 —— 说明当天的货都报量了。

---

## 六、出问题怎么办

**先双击 `diagnose.bat`**，它会生成 `diagnose-result.txt`，
**把这个文件发回给维护的人**。它不需要 Python 也能跑，所以哪怕
「Python 没装好」这种情况也照样能收集信息。

常见的几种：

| 现象 | 怎么办 |
|---|---|
| 双击 `install.bat` 说找不到 Python | 装 Python 时漏勾了 `Add python.exe to PATH`。重装一次并勾上 |
| 双击 `start.bat` 说「等待超时」 | 等 30 秒，再双击一次。还不行就发 `diagnose-result.txt` |
| 控制台打不开、浏览器白屏 | 双击 `start.bat`；还不行就双击 `stop.bat` 再 `start.bat` |
| 「会话未导入」/ 提示要重新登录 | 到「会话」页点「打开浏览器抓取」，重新登一次 |
| 抓取时说「浏览器关掉了」 | **别关那个登录窗口**。重新点一次「打开浏览器抓取」 |
| 报告里的数字不对 | 先看「设置 → 门店」那三行配对没有（`selftest.bat` 第 0 节会打印当前配置的门店） |

---

## 七、升级 / 卸载

**升级不用你管** —— 控制台每天会自己检查，有新版本会在「设置」里挂提醒，
点一下「立即更新」就行（只换程序，账号、会话、历史报告都不会动）。

**卸载**：双击 `uninstall.bat`。它**默认不删**报告和历史数据，会先问你。
